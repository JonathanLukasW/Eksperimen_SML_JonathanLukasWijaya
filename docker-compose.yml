version: '3.8'

services:
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus_msml
    volumes:
      - "./Monitoring dan Logging/prometheus.yml:/etc/prometheus/prometheus.yml"
      - prometheus-storage:/prometheus
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana:latest
    container_name: grafana_msml
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_SMTP_ENABLED=true
      - GF_SMTP_HOST=smtp.gmail.com:587
      - GF_SMTP_USER=jonathanlukas17116@gmail.com
      - GF_SMTP_PASSWORD=wteq kdnn vuxf wqyw
      - GF_SMTP_FROM_ADDRESS=jonathanlukas17116@gmail.com
      - GF_SMTP_FROM_NAME=Grafana MSML Alert
      - GF_SMTP_STARTTLS_POLICY=MandatoryStartTLS
      - GF_SMTP_SKIP_VERIFY=true

volumes:
  prometheus-storage:
  grafana-storage: